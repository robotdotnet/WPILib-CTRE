// Include and src directories for building the driver library
ext.driverInclude = "${rootDir}/include"
ext.driverSrc = "${rootDir}/src"

// Folder to put release zips into
ext.releaseDir = file("${rootDir}/release")
